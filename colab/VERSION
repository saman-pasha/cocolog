4
# colab/VERSION -- the notebook's revision, and the ONE PLACE it is declared.
#
# The first line is the version. Everything below is a log.
#
# WHY A FILE AND NOT JUST A NUMBER IN THE NOTEBOOK. The notebook you are
# running lives in your BROWSER, and the repository is what `git reset
# --hard' updates. Those two are different copies and they have already
# disagreed: the package list was corrected upstream, the cell in the
# browser stayed as it was, and the same run failed twice for the same
# reason with a corrected fix sitting on disk the whole time.
#
# So the number is declared HERE, the notebook carries the number it was
# published at, and section 1 compares them and says so. A version you
# can read is worth something; a version that CHECKS ITSELF against the
# repository is worth the thing that actually went wrong.
#
# Bump this whenever a notebook CELL changes. A change to prereqs.sh,
# preflight.sh or build.sh does not need a bump -- those come from the
# repository already, which is the point of their being scripts.
#
# 4  2026-08-26  every cell names the cocolog binary by its FULL path.
#                A cell inherits the last cell's working directory, and
#                section 4 used to %cd into ZiguratIP and back -- so
#                section 2, which runs first, hit `./cocolog: Is a
#                directory'. No cell depends on execution order now.
# 3  2026-08-26  the environment is set BEFORE prereqs.sh runs, because
#                prereqs.sh reads CICILI to tell ASDF where the Cicili
#                checkout is. It used to fall back to its own default,
#                which was right by luck. And prereqs.sh provisions the
#                LISP side at all: Quicklisp for str and cl-ppcre, the
#                two shims Quicklisp does not have, and the symlink that
#                lets ASDF find the Cicili checkout.
# 2  2026-08-26  the build refusal carries its cause, the way the
#                preflight refusal already did -- streamed AND captured,
#                so a long build is watchable and a pasted tail still
#                names what failed. preflight asks the DRIVER before it
#                asks torch, so "no GPU" stops blaming the Runtime menu
#                on a machine already in GPU mode.
# 1  2026-08-26  the first version that carries a version.
